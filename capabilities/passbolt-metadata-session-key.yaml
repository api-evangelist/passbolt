naftiko: 1.0.0-alpha2
info:
  label: Passbolt API — Metadata session key
  description: 'Passbolt API — Metadata session key. 4 operations. Lead operation: Update a given session-key entry.. Self-contained
    Naftiko capability covering one Passbolt business surface.'
  tags:
  - Passbolt
  - Metadata session key
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    PASSBOLT_API_KEY: PASSBOLT_API_KEY
capability:
  consumes:
  - type: http
    namespace: passbolt-metadata-session-key
    baseUri: https://passbolt.local
    description: Passbolt API — Metadata session key business capability. Self-contained, no shared references.
    resources:
    - name: metadata-session-key-sessionKeyId}.json
      path: /metadata/session-key/{sessionKeyId}.json
      operations:
      - name: updatemetadatasessionkey
        method: POST
        description: Update a given session-key entry.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: deletesessionkey
        method: DELETE
        description: Delete a given session-key entry.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: metadata-session-keys.json
      path: /metadata/session-keys.json
      operations:
      - name: viewmetadatasessionkeys
        method: GET
        description: Get session keys.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: addmetadatasessionkey
        method: POST
        description: Add a session key.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.PASSBOLT_API_KEY}}'
  exposes:
  - type: rest
    namespace: passbolt-metadata-session-key-rest
    port: 8080
    description: REST adapter for Passbolt API — Metadata session key. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/metadata/session-key/sessionkeyid-json
      name: metadata-session-key-sessionkeyid-json
      description: REST surface for metadata-session-key-sessionKeyId}.json.
      operations:
      - method: POST
        name: updatemetadatasessionkey
        description: Update a given session-key entry.
        call: passbolt-metadata-session-key.updatemetadatasessionkey
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletesessionkey
        description: Delete a given session-key entry.
        call: passbolt-metadata-session-key.deletesessionkey
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/metadata/session-keys-json
      name: metadata-session-keys-json
      description: REST surface for metadata-session-keys.json.
      operations:
      - method: GET
        name: viewmetadatasessionkeys
        description: Get session keys.
        call: passbolt-metadata-session-key.viewmetadatasessionkeys
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: addmetadatasessionkey
        description: Add a session key.
        call: passbolt-metadata-session-key.addmetadatasessionkey
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: passbolt-metadata-session-key-mcp
    port: 9090
    transport: http
    description: MCP adapter for Passbolt API — Metadata session key. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: update-given-session-key-entry
      description: Update a given session-key entry.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: passbolt-metadata-session-key.updatemetadatasessionkey
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-given-session-key-entry
      description: Delete a given session-key entry.
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: passbolt-metadata-session-key.deletesessionkey
      outputParameters:
      - type: object
        mapping: $.
    - name: get-session-keys
      description: Get session keys.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: passbolt-metadata-session-key.viewmetadatasessionkeys
      outputParameters:
      - type: object
        mapping: $.
    - name: add-session-key
      description: Add a session key.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: passbolt-metadata-session-key.addmetadatasessionkey
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
